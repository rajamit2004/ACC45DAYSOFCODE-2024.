#include <iostream>
using namespace std;

int main() {
    int T;
    cin >> T;

    while (T--) {
        int N, X;
        cin >> N >> X;

        int faceDown = N - X;
        int minFlips = min(X, faceDown);
         cout << minFlips << endl;
    }

    return 0;
}
