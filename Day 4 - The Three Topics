#include <iostream>
using namespace std;

int main() {
#include <iostream>

    int A, B, C, X;
    cin >> A >> B >> C >> X;

    if (X == A || X == B || X == C) {
        cout << "Yes" <<endl;
    } else {
        cout << "No" <<endl;
    }

    return 0;
}
